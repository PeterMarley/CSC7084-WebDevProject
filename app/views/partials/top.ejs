<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <!--<meta name="viewport" content="width=device-width, initial-scale=1.0">-->
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>Moodr</title>
  <link href="/styles/styles.css" rel="stylesheet">
  <!-- <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css"> -->
  <link rel="icon" type="image/x-icon" href="/icons/main_icon_16x16.png">
  <script>
    const usernameRegex = /<%- config.userDetailsValidation.username.regex %>/;
    const passwordRegex = /<%- config.userDetailsValidation.password.regex %>/;
    const emailRegex = /<%- config.userDetailsValidation.email.regex %>/;
    const username = '<%= (typeof username !== "undefined") ? username : "unknown" %>';
  </script>
  <script src="/script/validator.js"></script>
</head>

<body>

  <div id="header" class="flex-container centered-v centered-h row">
    <div class="modal-container hidden" id="hamburger-modal">
      <div class="modal">
        <div class="modal-close-button">✕</div>
        <% if (authed) { // if authenticated%>
        <%- include('../components/user-details-container') %>
        <% } else { // if not authenticated%>
        <!-- <div id="login-form-container" hidden> -->
        <%- include('../components/login-form'); %>
        <!-- </div> -->
        <% } %>
      </div>
    </div>
    <!-- TODO this link doesnt work when served from routes, eg /mood/ -->
    <div class="flex-container centered-v centered-h">
      <a href="/" class="flex-container centered-v centered-h">
        <img id="home-link-img" src="/icons/main_icon_128x128.png" alt="Moodr Icon">
      </a>
      <!-- <img  class="theme"> -->
      <div class="theme-change-button"></div>
      <div id="title-text"><span>MOOD</span><span>r</span></div>
    </div>
    <div class="hamburger-source button-style-1" id="hamburger-container">
      <div id="hamburger" class="hamburger-source">
        <!-- <img src="/icons/hamburger-black24x24.png" class="hamburger-source"> -->
      </div>
    </div>
    <script src="/script/header.js"></script>
  </div>
  <div id="body" class="flex-container row flex-grow-1">
    <%- include('nav') %>
    <div id="content-div" class="flex-container flex-grow-1 centered-h">